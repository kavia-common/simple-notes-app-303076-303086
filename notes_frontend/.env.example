# Optional: override where the frontend sends API requests.
# If not set, the app defaults to: `${window.location.protocol}//${window.location.hostname}:3001`
# This matches the typical local/KAVIA setup: frontend on :3000, backend on :3001
REACT_APP_API_BASE=http://localhost:3001
